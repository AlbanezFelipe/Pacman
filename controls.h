#pragma once

#include <stdbool.h>

extern bool isRunning;

extern bool keyArrowUp;
extern bool keyArrowDown;
extern bool keyArrowRight;
extern bool keyArrowLeft;

void eventListener(void); //Update Events
